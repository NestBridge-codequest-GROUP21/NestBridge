/**
 * @deprecated Import from `ghanaReference.ts` instead.
 * Re-exports for backwards compatibility.
 */
export {
  GHANA_PLACES,
  normalizePlaceToken,
  isKnownGhanaPlace,
  normalizeCity,
  destinationCityOptions,
  universityOptionsForCity,
  destinationPlaceholderExamples,
} from './ghanaReference';
